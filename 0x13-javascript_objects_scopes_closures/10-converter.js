#!/usr/bin/node
exports.converter = function (base) {
  if (base === 10 {
    console.log(base);
  }else if(base === 16) {
    console.log(base.toString(16));
  }else {
	while(base % base) {
	  console.log(base)
	}
  }
}
